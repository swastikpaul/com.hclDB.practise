CREATE TABLE employee(
   ID          INT NOT NULL,
   NAME        VARCHAR (20) NOT NULL,
   GENDER      VARCHAR (5) NOT NULL,
   AGE 		   INT (100),
   DOB         DATE,
   SALARY 	   INT (100),
   ADDRESS     VARCHAR (100),
   PRIMARY KEY (ID)
);