CREATE TABLE company(
   ID          		 INT NOT NULL,
   C_NAME       	 VARCHAR (20) NOT NULL,
   SECTOR      		 VARCHAR (20),
   SINCE 		     INT (10),
   NO_OF_EMPLOYEE 	 INT (100),
   LOCATION		     VARCHAR (100),
   PRIMARY KEY (ID)
);